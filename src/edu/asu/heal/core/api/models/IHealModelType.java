package edu.asu.heal.core.api.models;

public interface IHealModelType {
    // Model used to specify types specific to the HEAL API
    // Currently implemented by Activity, ActivityInstance, Domain, Patient, Trial, NotificationData, NotificationRequestModel, Logger
    // If a new model is added, please make sure that it somehow implements this interface
}
